import React from 'react';
import styles from '../footer/footer.module.css'
const Footer = (props) => (
            <footer className={styles.footer}>Hyomin's React Practice</footer>
    );

export default Footer;